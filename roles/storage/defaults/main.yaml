---
NFS_packages:
  - lvm2
  - nfs-utils

shared_disk: /dev/vdb

shared_vg: vg_shared
shared_lv: lv_shared

shared_lv_size: 5G

shared_fs_type: xfs

nfs_service: nfs-server

nfs_export_options: rw,sync,no_root_squash
...
